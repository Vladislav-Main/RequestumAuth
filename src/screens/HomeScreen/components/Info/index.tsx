import React from 'react';
import {StyledInfoView, StyledInfo} from './styles';

const Info = () => {
  return (
    <StyledInfoView>
      <StyledInfo>Now you can see the app content!</StyledInfo>
    </StyledInfoView>
  );
};

export default Info;
